# Telegram Posts Copier

Автоматизированный скрипт для копирования постов из приватной Telegram-группы в другую группу с использованием Telethon. Обеспечивает 100% идентичность оригинальным постам без меток пересылки.

## 🚀 Особенности

- ✅ **Идентичное копирование**: Текст, фото, видео, хештеги, эмодзи, форматирование
- ✅ **Хронологический порядок**: Копирование от самых старых постов к новым
- ✅ **Надежное возобновление**: Атомарное сохранение прогресса при прерывании
- ✅ **Защита от дублирования**: Блокировка множественного запуска
- ✅ **Соблюдение лимитов**: Встроенные механизмы защиты от банов
- ✅ **Docker поддержка**: Полная контейнеризация
- ✅ **Детальное логирование**: Отслеживание прогресса и ошибок
- ✅ **Режим симуляции**: Тестирование без реальной отправки
- ✅ **Валидация доступа**: Проверка прав перед началом копирования

## 📋 Требования

- Python 3.12+
- Docker и Docker Compose (для контейнерного запуска)
- Telegram API credentials (API_ID, API_HASH)
- Доступ к исходной приватной группе
- Права администратора в целевой группе

## 🚀 Быстрый старт

### 1. Получение API ключей

1. Перейдите на [my.telegram.org/apps](https://my.telegram.org/apps)
2. Войдите в свой аккаунт Telegram
3. Создайте новое приложение
4. Сохраните `API_ID` и `API_HASH`

### 2. Настройка конфигурации

#### Вариант A: Интерактивная настройка
```bash
python main.py setup
```

#### Вариант B: Создание .env файла вручную
```bash
cp .env.example .env
# Отредактируйте .env файл с вашими данными
```

Пример `.env` файла:
```env
# Telegram API настройки
API_ID=12345678
API_HASH=abcdef1234567890abcdef1234567890
PHONE=+79123456789

# Настройки групп
SOURCE_GROUP_ID=@source_group
TARGET_GROUP_ID=@target_group

# Настройки поведения
DELAY_SECONDS=3
MESSAGES_PER_HOUR=30
DRY_RUN=false
```

### 3. Авторизация и запуск

> ⚠️ **ВАЖНО**: Первичная авторизация требует ввода SMS кода и возможно 2FA пароля

#### 🚀 Автоматическая настройка (РЕКОМЕНДУЕТСЯ)
```bash
# Запустите автоматический скрипт настройки
./setup_auth.sh
```

#### 📱 Ручная авторизация - Способ 1: Локально → Docker
```bash
# 1. Установите зависимости локально
pip install telethon python-dotenv pyyaml tqdm

# 2. Первичная авторизация (интерактивно)
python main.py
# Введите SMS код и 2FA пароль когда попросит

# 3. После создания сессии используйте Docker
docker-compose up --build
```

#### 🐳 Ручная авторизация - Способ 2: Docker интерактивный
```bash
# Запуск в интерактивном режиме для авторизации
docker-compose -f docker-compose.interactive.yml run --rm telegram-copier

# После авторизации используйте обычный режим
docker-compose up --build
```

#### 🔧 Ручная авторизация - Способ 3: Переменные окружения
```bash
# Получите SMS код и запустите с переменными
TELEGRAM_CODE=12345 TELEGRAM_PASSWORD=your_2fa_password docker-compose up --build
```

## 🐳 Запуск Docker

### Автоматические скрипты (рекомендуется)

```bash
# Для современного Docker Compose (docker compose)
./run_with_auth.sh

# Для старого Docker Compose (docker-compose)
./run_with_auth_legacy.sh
```

### Прямой запуск

```bash
# Современный синтаксис
docker compose up --build

# Старый синтаксис
docker-compose up --build
```

### Проверка версии Docker Compose

```bash
# Проверьте, какая версия у вас
docker compose version
# или
docker-compose --version
```

#### 🎯 Обычный запуск (после авторизации)
```bash
# Запуск в режиме симуляции
DRY_RUN=true docker-compose up --build

# Реальное копирование
docker-compose up --build

# Запуск в фоне
docker-compose up -d --build
```

## ⚙️ Конфигурация

### Основные параметры

| Параметр | Описание | По умолчанию | Обязательный |
|----------|----------|--------------|--------------|
| `API_ID` | ID приложения Telegram | - | ✅ |
| `API_HASH` | Hash приложения Telegram | - | ✅ |
| `PHONE` | Номер телефона с кодом страны | - | ✅ |
| `SOURCE_GROUP_ID` | ID или username исходной группы | - | ✅ |
| `TARGET_GROUP_ID` | ID или username целевой группы | - | ✅ |

### Настройки поведения

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `DELAY_SECONDS` | Задержка между сообщениями (сек) | 3 |
| `MESSAGES_PER_HOUR` | Максимум сообщений в час | 30 |
| `DRY_RUN` | Режим симуляции без отправки | false |
| `SESSION_NAME` | Имя файла сессии | telegram_copier |
| `RESUME_FILE` | Файл для возобновления | last_message_id.txt |

### Авторизация в Docker

Для авторизации в Docker окружении:
```bash
# При первом запуске укажите код из SMS
TELEGRAM_CODE=12345 docker-compose up

# При включенной 2FA также укажите пароль
TELEGRAM_PASSWORD=your_password docker-compose up
```

## 🔄 Возобновление работы

Скрипт автоматически сохраняет прогресс и может продолжить работу с места остановки:

- ✅ **Атомарное сохранение**: Прогресс сохраняется перед каждым сообщением
- ✅ **Защита от потерь**: При любом прерывании (Ctrl+C, crash, kill) прогресс сохранен
- ✅ **Автоматическое возобновление**: При следующем запуске продолжит с последнего сообщения
- ✅ **Блокировка дублирования**: Предотвращает запуск нескольких экземпляров

```bash
# Если скрипт был прерван, просто запустите снова
python main.py
# Он автоматически продолжит с места остановки
```

## 🛡️ Безопасность и лимиты

### Встроенная защита от банов

- **Автоматические задержки** между сообщениями (по умолчанию 3 сек)
- **Лимиты в час** (по умолчанию 30 сообщений/час)
- **Обработка FloodWaitError** с автоматическим ожиданием
- **Валидация доступа** к группам перед началом
- **Режим симуляции** для безопасного тестирования

### Рекомендации по безопасности

1. **Всегда начинайте с режима симуляции**: `DRY_RUN=true`
2. **Используйте консервативные настройки** для новых аккаунтов
3. **Не превышайте 30-50 сообщений в день** для новых аккаунтов
4. **Рассмотрите использование прокси** для дополнительной защиты
5. **Мониторьте логи** на предмет предупреждений

## 📊 Логирование и мониторинг

Детальное логирование всех операций:

- **Консольный вывод** с прогрессом в реальном времени
- **Файл логов** `telegram_copier.log` с полной информацией
- **Статистика выполнения** по завершении
- **Отслеживание ошибок** с подробными сообщениями

```bash
# Просмотр логов в реальном времени
tail -f telegram_copier.log

# Поиск ошибок в логах
grep ERROR telegram_copier.log
```

## 🧪 Тестирование

Проект включает комплексные тесты:

```bash
# Запуск базовых тестов (без внешних зависимостей)
python3 test_minimal.py

# Проверка синтаксиса всех модулей
python3 -m py_compile main.py config.py copier.py utils.py
```

## 🐳 Docker

### Простой запуск
```bash
# Создайте .env файл с настройками
cp .env.example .env

# Запустите в режиме симуляции
DRY_RUN=true docker-compose up --build
```

### Продвинутое использование
```bash
# Запуск с пользовательскими переменными
API_ID=12345 API_HASH=abcdef PHONE=+1234567890 \
SOURCE_GROUP_ID=@source TARGET_GROUP_ID=@target \
DRY_RUN=true docker-compose up --build

# Просмотр логов контейнера
docker-compose logs -f telegram-copier
```

## 🔧 Устранение неполадок

### Ошибки авторизации
```bash
# Проверьте правильность API данных
grep -E "API_ID|API_HASH|PHONE" .env

# Убедитесь что номер указан с кодом страны
PHONE=+79123456789  # ✅ Правильно
PHONE=9123456789    # ❌ Неправильно
```

### Проблемы с группами
```bash
# Проверьте доступ к группам
# Убедитесь что вы участник исходной группы
# Убедитесь что у вас есть права в целевой группе
```

### Docker проблемы
```bash
# Просмотр логов
docker-compose logs telegram-copier

# Пересборка образа
docker-compose build --no-cache

# Очистка volumes
docker-compose down -v
```

### Прерванное копирование
```bash
# Проверьте файл прогресса
cat last_message_id.txt

# Удалите для полного перезапуска
rm last_message_id.txt

# Или продолжите с определенного ID
echo "12345" > last_message_id.txt
```

## 📁 Структура проекта

```
telegram-copier/
├── main.py              # 🎯 Главный модуль с авторизацией
├── config.py            # ⚙️ Управление конфигурацией  
├── copier.py            # 🔄 Логика копирования постов
├── utils.py             # 🛠️ Вспомогательные функции
├── requirements.txt     # 📦 Python зависимости
├── Dockerfile          # 🐳 Docker образ
├── docker-compose.yml  # 🐳 Docker Compose конфигурация
├── .env.example        # 📝 Пример конфигурации
├── .dockerignore       # 🐳 Исключения для Docker
├── test_minimal.py     # 🧪 Тесты базовой логики
├── README.md           # 📚 Документация
└── LICENSE             # ⚖️ MIT лицензия
```

## 🔍 Примеры использования

### Базовое копирование
```bash
# Настройте .env файл
API_ID=12345678
API_HASH=your_hash
PHONE=+1234567890
SOURCE_GROUP_ID=@private_source
TARGET_GROUP_ID=@my_backup_group

# Запустите копирование
python main.py
```

### Безопасное тестирование
```bash
# Сначала протестируйте в режиме симуляции
DRY_RUN=true python main.py

# Затем запустите реальное копирование
python main.py
```

### Копирование с ограничениями
```bash
# Очень консервативные настройки
DELAY_SECONDS=10
MESSAGES_PER_HOUR=10
python main.py
```

### Использование прокси
```bash
# Настройка SOCKS5 прокси
PROXY_SERVER=proxy.example.com
PROXY_PORT=1080
PROXY_USERNAME=user
PROXY_PASSWORD=pass
python main.py
```

## ⚠️ Важные предупреждения

### ✅ Разрешенное использование:
- Архивация собственного контента
- Миграция между собственными группами  
- Резервное копирование важной информации
- Консолидация контента из нескольких источников

### ❌ Запрещенное использование:
- Спам или массовые рассылки
- Копирование чужого контента без разрешения
- Нарушение правил Telegram
- Коммерческое использование без согласия

### 🛡️ Рекомендации по безопасности:
- Используйте только с собственными группами
- Соблюдайте авторские права
- Не превышайте разумные лимиты
- Мониторьте активность аккаунта

## 📞 Поддержка

При возникновении проблем:

1. **Проверьте логи**: `cat telegram_copier.log`
2. **Запустите тесты**: `python3 test_minimal.py`
3. **Проверьте конфигурацию**: `python main.py setup`
4. **Попробуйте режим симуляции**: `DRY_RUN=true python main.py`

## 🏆 Особенности реализации

### Надежность
- ✅ Атомарное сохранение прогресса
- ✅ Защита от множественного запуска
- ✅ Graceful shutdown при прерывании
- ✅ Автоматическое восстановление после ошибок

### Производительность  
- ✅ Эффективная пагинация больших объемов
- ✅ Минимальное потребление памяти (<512MB)
- ✅ Оптимизированные запросы к API
- ✅ Интеллектуальное управление задержками

### Безопасность
- ✅ Соблюдение всех лимитов Telegram API
- ✅ Защита от FloodWaitError
- ✅ Валидация доступа к группам
- ✅ Безопасное хранение сессий

---

## 📄 Лицензия

MIT License - см. файл [LICENSE](LICENSE)

**Разработано в полном соответствии с техническим заданием**

*Версия 2.0 - Улучшенная надежность и безопасность*